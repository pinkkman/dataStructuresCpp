#include <iostream>
using namespace std;
int bin(int n)
{
  int pow = 1, b, ans = 0;
  while (n > 0)
  {
    b = n % 2 ;
    n=n/2;
    ans = ans + (b*pow);
    pow = pow * 10;
  }
  return ans;
}

int main()
{
  int n;
  cout << "enter n : ";
  cin >> n;
cout<<bin(n);
  return 0;
}